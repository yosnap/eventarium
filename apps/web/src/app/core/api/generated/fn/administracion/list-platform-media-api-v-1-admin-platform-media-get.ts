/* eslint-disable */
/* Code generated by ng-openapi-gen DO NOT EDIT. */

import { HttpClient, HttpContext, HttpResponse } from '@angular/common/http';
import { Observable } from 'rxjs';
import { filter, map } from 'rxjs/operators';
import { StrictHttpResponse } from '../../strict-http-response';
import { RequestBuilder } from '../../request-builder';

import { PageMediaResponse } from '../../models/page-media-response';

export interface ListPlatformMediaApiV1AdminPlatformMediaGet$Params {
  search?: (string | null);
  folder_id?: (string | null);

/**
 * Elementos por página
 */
  limit?: number;

/**
 * Elementos omitidos
 */
  offset?: number;
}

export function listPlatformMediaApiV1AdminPlatformMediaGet(http: HttpClient, rootUrl: string, params?: ListPlatformMediaApiV1AdminPlatformMediaGet$Params, context?: HttpContext): Observable<StrictHttpResponse<PageMediaResponse>> {
  const rb = new RequestBuilder(rootUrl, listPlatformMediaApiV1AdminPlatformMediaGet.PATH, 'get');
  if (params) {
    rb.query('search', params.search, {});
    rb.query('folder_id', params.folder_id, {});
    rb.query('limit', params.limit, {});
    rb.query('offset', params.offset, {});
  }

  return http.request(
    rb.build({ responseType: 'json', accept: 'application/json', context })
  ).pipe(
    filter((r: any): r is HttpResponse<any> => r instanceof HttpResponse),
    map((r: HttpResponse<any>) => {
      return r as StrictHttpResponse<PageMediaResponse>;
    })
  );
}

listPlatformMediaApiV1AdminPlatformMediaGet.PATH = '/api/v1/admin/platform/media';
