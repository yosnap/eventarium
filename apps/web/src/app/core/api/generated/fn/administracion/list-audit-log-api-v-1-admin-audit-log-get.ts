/* eslint-disable */
/* Code generated by ng-openapi-gen DO NOT EDIT. */

import { HttpClient, HttpContext, HttpResponse } from '@angular/common/http';
import { Observable } from 'rxjs';
import { filter, map } from 'rxjs/operators';
import { StrictHttpResponse } from '../../strict-http-response';
import { RequestBuilder } from '../../request-builder';

import { PageAuditLogEntry } from '../../models/page-audit-log-entry';

export interface ListAuditLogApiV1AdminAuditLogGet$Params {
  organization_id?: (string | null);
  date_from?: (string | null);
  date_to?: (string | null);
  action?: (string | null);

/**
 * Elementos por página
 */
  limit?: number;

/**
 * Elementos omitidos
 */
  offset?: number;
}

export function listAuditLogApiV1AdminAuditLogGet(http: HttpClient, rootUrl: string, params?: ListAuditLogApiV1AdminAuditLogGet$Params, context?: HttpContext): Observable<StrictHttpResponse<PageAuditLogEntry>> {
  const rb = new RequestBuilder(rootUrl, listAuditLogApiV1AdminAuditLogGet.PATH, 'get');
  if (params) {
    rb.query('organization_id', params.organization_id, {});
    rb.query('date_from', params.date_from, {});
    rb.query('date_to', params.date_to, {});
    rb.query('action', params.action, {});
    rb.query('limit', params.limit, {});
    rb.query('offset', params.offset, {});
  }

  return http.request(
    rb.build({ responseType: 'json', accept: 'application/json', context })
  ).pipe(
    filter((r: any): r is HttpResponse<any> => r instanceof HttpResponse),
    map((r: HttpResponse<any>) => {
      return r as StrictHttpResponse<PageAuditLogEntry>;
    })
  );
}

listAuditLogApiV1AdminAuditLogGet.PATH = '/api/v1/admin/audit-log';
