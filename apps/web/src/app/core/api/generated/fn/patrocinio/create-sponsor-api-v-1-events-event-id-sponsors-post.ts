/* eslint-disable */
/* Code generated by ng-openapi-gen DO NOT EDIT. */

import { HttpClient, HttpContext, HttpResponse } from '@angular/common/http';
import { Observable } from 'rxjs';
import { filter, map } from 'rxjs/operators';
import { StrictHttpResponse } from '../../strict-http-response';
import { RequestBuilder } from '../../request-builder';

import { SponsorCreate } from '../../models/sponsor-create';
import { SponsorResponse } from '../../models/sponsor-response';

export interface CreateSponsorApiV1EventsEventIdSponsorsPost$Params {
  event_id: string;
      body: SponsorCreate
}

export function createSponsorApiV1EventsEventIdSponsorsPost(http: HttpClient, rootUrl: string, params: CreateSponsorApiV1EventsEventIdSponsorsPost$Params, context?: HttpContext): Observable<StrictHttpResponse<SponsorResponse>> {
  const rb = new RequestBuilder(rootUrl, createSponsorApiV1EventsEventIdSponsorsPost.PATH, 'post');
  if (params) {
    rb.path('event_id', params.event_id, {});
    rb.body(params.body, 'application/json');
  }

  return http.request(
    rb.build({ responseType: 'json', accept: 'application/json', context })
  ).pipe(
    filter((r: any): r is HttpResponse<any> => r instanceof HttpResponse),
    map((r: HttpResponse<any>) => {
      return r as StrictHttpResponse<SponsorResponse>;
    })
  );
}

createSponsorApiV1EventsEventIdSponsorsPost.PATH = '/api/v1/events/{event_id}/sponsors';
