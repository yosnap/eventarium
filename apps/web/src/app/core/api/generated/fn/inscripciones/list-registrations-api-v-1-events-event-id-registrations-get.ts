/* eslint-disable */
/* Code generated by ng-openapi-gen DO NOT EDIT. */

import { HttpClient, HttpContext, HttpResponse } from '@angular/common/http';
import { Observable } from 'rxjs';
import { filter, map } from 'rxjs/operators';
import { StrictHttpResponse } from '../../strict-http-response';
import { RequestBuilder } from '../../request-builder';

import { PageRegistrationListItem } from '../../models/page-registration-list-item';

export interface ListRegistrationsApiV1EventsEventIdRegistrationsGet$Params {
  event_id: string;
  status?: ('pending_verification' | 'pending_approval' | 'pending_payment' | 'confirmed' | 'rejected' | 'cancelled' | 'waitlisted' | null);

/**
 * Elementos por página
 */
  limit?: number;

/**
 * Elementos omitidos
 */
  offset?: number;
}

export function listRegistrationsApiV1EventsEventIdRegistrationsGet(http: HttpClient, rootUrl: string, params: ListRegistrationsApiV1EventsEventIdRegistrationsGet$Params, context?: HttpContext): Observable<StrictHttpResponse<PageRegistrationListItem>> {
  const rb = new RequestBuilder(rootUrl, listRegistrationsApiV1EventsEventIdRegistrationsGet.PATH, 'get');
  if (params) {
    rb.path('event_id', params.event_id, {});
    rb.query('status', params.status, {});
    rb.query('limit', params.limit, {});
    rb.query('offset', params.offset, {});
  }

  return http.request(
    rb.build({ responseType: 'json', accept: 'application/json', context })
  ).pipe(
    filter((r: any): r is HttpResponse<any> => r instanceof HttpResponse),
    map((r: HttpResponse<any>) => {
      return r as StrictHttpResponse<PageRegistrationListItem>;
    })
  );
}

listRegistrationsApiV1EventsEventIdRegistrationsGet.PATH = '/api/v1/events/{event_id}/registrations';
